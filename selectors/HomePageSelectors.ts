/**
 * HomePage has no selectors of its own beyond the shared header controls already covered by
 * `BaseSelectors` (search field, search button, search toggle, consent banners). This file
 * exists for structural consistency with the other page/selector pairs and is the natural
 * place to add homepage-specific locators if the page grows.
 */
export const HomePageSelectors = {} as const;
